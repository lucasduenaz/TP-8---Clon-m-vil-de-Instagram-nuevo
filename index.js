import { registerRootComponent } from 'expo';
import App from './App';

// Registra el componente raíz de la aplicación con Expo.
// Esto garantiza que el entorno esté configurado correctamente
// tanto en Expo Go como en una compilación nativa.
registerRootComponent(App);
